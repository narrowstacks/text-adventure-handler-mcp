version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    environment:
      - DB_PATH=/data/adventure.db
    volumes:
      - ${HOST_DB_PATH}:/data/adventure.db:ro

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
